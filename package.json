{
  "name": "load-config",
  "version": "0.0.0",
  "description": "Load config",
  "main": "lib/load-config.js",
  "author": "Lloyd Brookes",
  "scripts": {
      "test": "tape test/*.js"
  },
  "dependencies": {
    "wodge": "~0.5.0"
  },
  "devDependencies": {
    "tape": "^2.12.3"
  },
  "config": {
      "one": "einen",
      "four": "package"
  }
}
